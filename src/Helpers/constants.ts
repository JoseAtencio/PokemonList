import {IRouteStack} from './types'

const ROUTE_STACK: IRouteStack = {
    RootInitNav: {
        loading: "root.loading",
        home: "root.home",
        list: "root.list",
        detail: "root.list.detail",
    },

    RootDev: {
        plantilla: "root.plantilla"
    }
};

export {
    ROUTE_STACK
}